// Matches Frontend/src/data/mockData.js PARKING_AREA.ratePerHour (40),
// used as the default rate for walk-in sessions that aren't tied to a
// priced ParkingSlot.
const PARKING_RATE_PER_HOUR = 40;

module.exports = { PARKING_RATE_PER_HOUR };
